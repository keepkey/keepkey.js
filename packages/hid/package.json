{
  "name": "@keepkey/hid",
  "version": "2.0.0",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "source": "src/index.ts",
  "umd:main": "dist/index.umd.js",
  "module": "dist/index.m.js",
  "scripts": {
    "build": "microbundle --name=keepkeyHID --external=crypto",
    "clean": "rm -rf dist .rts2_*",
    "dev": "microbundle watch --name=keepkeyHID --compress=false --external=crypto",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "dependencies": {
    "@keepkey/core": "^2.0.0",
    "@keepkey/keepkey.js": "^2.0.0",
    "bytebuffer": "^5.0.1",
    "eventemitter3": "^3.1.0",
    "node-hid": "^0.7.7",
    "usb-detection": "^4.1.0"
  },
  "devDependencies": {
    "@types/bytebuffer": "^5.0.40",
    "@types/eventemitter3": "^2.0.2",
    "@types/node-hid": "^0.7.0",
    "microbundle": "^0.9.0"
  }
}
